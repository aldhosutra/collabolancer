const { CONSTANTS } = require("./constants");
const { EXTENDEDAPI_CONFIG } = require("./extendedAPI");
const genesisBlockDevnet = require("./genesis_block.json");
const configDevnet = require("./config.json");

module.exports = {
  CONSTANTS,
  EXTENDEDAPI_CONFIG,
  genesisBlockDevnet,
  configDevnet,
};
